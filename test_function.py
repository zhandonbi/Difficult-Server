# 此文件用来测试每个功能模块
# 为防止文件路径以及其它问题，请在此文件内测试您的功能模块
from db_operator.item_db import ItemDb
from ASR.ASR import ASR
from AIR. import *
import base64
from io import BufferedReader, BytesIO
from PIL import Image
import random

if __name__ == '__main__':
    print('正在运行功能测试，注意不要使用在正式环境中')
    #with open('AIR/HDF5/selfPIC.jpg', 'rb') as f:
        # img_b64encode = base64.b64encode(f.read())  # base64编码
        # img_b64decode = base64.b64decode(img_b64encode)  # base64解码
        # image = io.BytesIO(img_b64decode)
        #print(AImage(f))
        #FN = ''.join(random.sample(
        #   ['z', 'y', 'x', 'w', 'v', 'u', 't', 's', 'r', 'q', 'p', 'o', 'n', 'm', 'l', 'k', 'j', 'i', 'h', 'g', 'f',
        #     'e', 'd', 'c', 'b', 'a'], 5))
    get_SM()
